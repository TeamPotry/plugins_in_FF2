"tutorial_text"
{
    "sapper_you_can_do_it"
    {
        "#format"   "{1:s}"
        "text"  "You may attach your sapper to enemy! Stun duration is {1} secs."

        "ko"
        {
            "text"  "전자 교란기를 적에게 붙일 수 있습니다! 스턴 지속시간은 {1}초입니다."
        }

        "cookie_rule"       "0"
        // 0 (Type_Normal) = text is no longer ignore rules. (But first message is will ignore SETTING.)
        // 1 (Type_OnlyOne) =  only show when first time. (SETTING ignored.)
        // 2 (Type_EveryTime) = if text following this rule, always show text. (SETTING ignored.)
        // 3 (Type_NormalEvenFirst) = text is no longer ignore rules.

        "show_effect"       "1"
        "show_distance"     "0"
        "id"                "10"    // custom_id for hide annotation.
        "lifetime"          "4.0"
        "play_sound"        "misc/talk.wav"   // Type sound path
    }
}
